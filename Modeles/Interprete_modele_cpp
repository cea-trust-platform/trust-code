//////////////////////////////////////////////////////////////////////////////
//
// File:	NOM_DU_FICHIER.cpp
// Directory:	$TRUST_ROOT/NOM_DU_MODULE
//
//////////////////////////////////////////////////////////////////////////////

#include <NOM_DU_FICHIER.h>
#include <Motcle.h>

implemente_instanciable(NOM_DE_LA_CLASSE,
                        "NOM_DE_LA_CLASSE",
                        Interprete)
   
// printOn et readOn 

Sortie& NOM_DE_LA_CLASSE::printOn(Sortie& s ) const 
{
   return s << que_suis_je() << "\n";
}

Entree& NOM_DE_LA_CLASSE::readOn(Entree& is ) 
{
//
// VERIFIER ICI QU'ON A BIEN TOUT LU;  
//
   return is;
}

Entree& NOM_DE_LA_CLASSE::interpreter(Entree& is)
{ 
   // DEFINISSEZ L'ACTION ASSOCIEE A VOTRE INTERPRETE
   // Exemple de lecture d'un domaine dom et d'un nom de fichier nom_fic
   // Nom nom_dom,nom_fic;
   // is >> nom_dom >> nom_fic;
   // if(! sub_type(Domaine, objet(nom_dom)))
   // {
   //    Cerr << nom_dom << " est du type " << objet(nom_dom).que_suis_je() << finl;
   //    Cerr << "On attendait un objet de type Domaine" << finl;
   //    exit(-1);
   // }
   // Domaine& dom=ref_cast(Domaine, objet(nom_dom));
   // SFichier fic(nom_fic);
   // Cerr << "Ecriture du fichier : " << nom_fic << finl;
   // if(!fic){
   //    Cerr << "Impossible d'ouvrir le fichier " << nom_fic << finl;
   //    Cerr << "Entrer un autre nom de fichier svp ... ";
   //    exit(-1);
   // }
   
   
   
  return is;
}
