
SET(_all_SCRIPTS
    trust_wiz_test.py
)

SALOME_INSTALL_SCRIPTS("${_all_SCRIPTS}" ${SALOME_INSTALL_SCRIPT_PYTHON}/tests)

SALOME_ACCUMULATE_ENVIRONMENT(PYTHONPATH ${PROJECT_BINARY_DIR}/src)  
SALOME_GENERATE_TESTS_ENVIRONMENT(tests_env)

ADD_TEST(TrustWizUnitTests ${PYTHON_EXECUTABLE} ${CMAKE_CURRENT_SOURCE_DIR}/trust_wiz_test.py)
SET_TESTS_PROPERTIES(TrustWizUnitTests PROPERTIES ENVIRONMENT "${tests_env}")
