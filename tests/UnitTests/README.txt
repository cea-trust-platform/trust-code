In this directory we place small unit tests for arrays, Kokkos, GPU stuff, etc.

The CMakeLists found here is not automatically generated.

Tests are based on the GoogleTest framework.

They can be compiled with
    make unit
at the TRUST root level, and then executed with
    exec/unit_gtest
