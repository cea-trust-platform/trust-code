# Appel pour tester tous les solveurs sur le cas test
Check_solver.sh -all
err=$?
if [ $err = 0 ]
then
   ranking=`ls -rt ranking.* | tail -1`
   if [ "`grep parallele $ranking`" = "" ]
   then
      ./check_ranking `ls -rt ranking.* | tail -1` 134 3.e-8 364 20
      err=$?
   else
      ./check_ranking `ls -rt ranking.* | tail -1` 144 3.e-8 511 13
      err=$?
   fi
fi
exit $err
