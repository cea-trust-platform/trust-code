#!/bin/bash
for f in $*
do
 [ ! -f $f ] && continue
 [ "`grep int $f`" = "" ] && continue
 [ -f $f.P ] && chmod gu+w $f
 $TRUST_ROOT/exec/python/bin/python replace_int_entier.py $f > $f.p
#	xterm
 cat $f.p > $f
rm $f.p
 [ -f $f.P ] && chmod gu-w $f
done 
