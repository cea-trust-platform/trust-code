#!/bin/bash
for f in $*
do
 [ ! -f $f ] && continue
 [ "`grep int $f`" = "" ] && continue
 [ -f $f.P ] && chmod gu+w $f
 python replace_int_entier.py $f > $f.p
#	xterm
 cat $f.p > $f
rm $f.p
 [ -f $f.P ] && chmod gu-w $f
done 
