file(GLOB srcs [A-Z|a-z]*.cpp [A-Z|a-z]*.c [A-Z|a-z]*.f)
include_directories(${CMAKE_CURRENT_SOURCE_DIR}  ${TRUST_ROOT}/include/backward  ${TRUST_ROOT}/src/Kernel/Champs  ${TRUST_ROOT}/src/Kernel/Framework  ${TRUST_ROOT}/src/Kernel/Geometrie  ${TRUST_ROOT}/src/Kernel/Math  ${TRUST_ROOT}/src/Kernel/Math/Matrices  ${TRUST_ROOT}/src/Kernel/Math/SolvSys  ${TRUST_ROOT}/src/Kernel/Utilitaires )
add_library(obj_op OBJECT  ${srcs} )
set(listlibs ${listlibs} op PARENT_SCOPE    )
add_custom_target(check_sources_op   COMMAND check_sources.sh ${CMAKE_CURRENT_SOURCE_DIR} ) #COMMENT  "checking code validity op" )
add_DEPENDENCIES(obj_op check_sources_op)
if(COMPIL_DYN)
  add_library(op SHARED   $<TARGET_OBJECTS:obj_op> )
  install(TARGETS op  DESTINATION lib)
endif(COMPIL_DYN)
