file(GLOB srcs [A-Z|a-z]*.cpp [A-Z|a-z]*.c [A-Z|a-z]*.f)
include_directories(${CMAKE_CURRENT_SOURCE_DIR}  ${TRUST_ROOT}/include/backward   ${TRUST_ROOT}/src/Kernel/Framework   ${TRUST_ROOT}/src/Kernel/Geometrie   ${TRUST_ROOT}/src/Kernel/Math   ${TRUST_ROOT}/src/Kernel/Math/Matrices   ${TRUST_ROOT}/src/Kernel/Math/SolvSys   ${TRUST_ROOT}/src/Kernel/Schemas_Temps   ${TRUST_ROOT}/src/Kernel/Utilitaires  )
add_library(obj_solv OBJECT  ${srcs} )
set(listlibs ${listlibs} solv PARENT_SCOPE    )
add_custom_target(check_sources_solv   COMMAND check_sources.sh ${CMAKE_CURRENT_SOURCE_DIR} COMMENT  "checking code validity solv" )
add_DEPENDENCIES(obj_solv check_sources_solv)
if(COMPIL_DYN)
  add_library(solv SHARED   $<TARGET_OBJECTS:obj_solv> )
  install(TARGETS solv  DESTINATION lib)
endif(COMPIL_DYN)
