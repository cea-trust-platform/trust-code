file(GLOB srcs [A-Z|a-z]*.cpp [A-Z|a-z]*.c [A-Z|a-z]*.f)
include_directories(${CMAKE_CURRENT_SOURCE_DIR}  ${TRUST_ROOT}/include/backward  ${MPI_INCLUDE}  ${TRUST_ROOT}/lib/src/LIBAMGX/AmgXWrapper/include  ${TRUST_ROOT}/lib/src/LIBAMGX/AmgX/include  ${CUDA_INC_PATH}  ${PETSC_ROOT}/${TRUST_ARCH}${OPT}/include  ${TRUST_ROOT}/src/Kernel/Math/Matrices  ${TRUST_ROOT}/src/Kernel/Math  ${TRUST_ROOT}/src/Kernel/Utilitaires )
add_library(obj_solvsys OBJECT  ${srcs} )
set(listlibs ${listlibs} solvsys PARENT_SCOPE    )
add_custom_target(check_sources_solvsys   COMMAND check_sources.sh ${CMAKE_CURRENT_SOURCE_DIR} ) #COMMENT  "checking code validity solvsys" )
add_DEPENDENCIES(obj_solvsys check_sources_solvsys)
if(COMPIL_DYN)
  add_library(solvsys SHARED   $<TARGET_OBJECTS:obj_solvsys> )
  install(TARGETS solvsys  DESTINATION lib)
endif(COMPIL_DYN)
