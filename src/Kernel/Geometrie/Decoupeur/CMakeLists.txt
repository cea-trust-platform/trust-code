file(GLOB srcs [A-Z|a-z]*.cpp [A-Z|a-z]*.c [A-Z|a-z]*.f)
include_directories(${CMAKE_CURRENT_SOURCE_DIR}  ${TRUST_ROOT}/include/backward   ${METIS_ROOT}/include   ${TRUST_ROOT}/src/Kernel/Framework   ${TRUST_ROOT}/src/Kernel/Geometrie   ${TRUST_ROOT}/src/Kernel/Math   ${TRUST_ROOT}/src/Kernel/Math/Matrices   ${TRUST_ROOT}/src/Kernel/Utilitaires  )
add_library(obj_geomdec OBJECT  ${srcs} )
set(listlibs ${listlibs} geomdec PARENT_SCOPE    )
add_custom_target(check_sources_geomdec   COMMAND check_sources.sh ${CMAKE_CURRENT_SOURCE_DIR} ) #COMMENT  "checking code validity geomdec" )
add_DEPENDENCIES(obj_geomdec check_sources_geomdec)
if(COMPIL_DYN)
  add_library(geomdec SHARED   $<TARGET_OBJECTS:obj_geomdec> )
  install(TARGETS geomdec  DESTINATION lib)
endif(COMPIL_DYN)
