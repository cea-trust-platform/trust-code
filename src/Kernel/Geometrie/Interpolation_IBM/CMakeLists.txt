file(GLOB srcs [A-Z|a-z]*.cpp [A-Z|a-z]*.c [A-Z|a-z]*.f)
include_directories(${CMAKE_CURRENT_SOURCE_DIR}  ${TRUST_ROOT}/include/backward  ${TRUST_ROOT}/src/Kernel/Framework  ${TRUST_ROOT}/src/Kernel/Geometrie  ${TRUST_ROOT}/src/Kernel/Math/Matrices  ${TRUST_ROOT}/src/Kernel/Math  ${TRUST_ROOT}/src/Kernel/Utilitaires\ )
add_library(obj_interp_ibm OBJECT  ${srcs} )
set(listlibs ${listlibs} interp_ibm PARENT_SCOPE    )
add_custom_target(check_sources_interp_ibm   COMMAND check_sources.sh ${CMAKE_CURRENT_SOURCE_DIR} ) #COMMENT  "checking code validity interp_ibm" )
add_DEPENDENCIES(obj_interp_ibm check_sources_interp_ibm)
if(COMPIL_DYN)
  add_library(interp_ibm SHARED   $<TARGET_OBJECTS:obj_interp_ibm> )
  install(TARGETS interp_ibm  DESTINATION lib)
endif(COMPIL_DYN)
