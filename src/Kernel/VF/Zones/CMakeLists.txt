file(GLOB srcs [A-Z|a-z]*.cpp [A-Z|a-z]*.c [A-Z|a-z]*.f)
include_directories(${CMAKE_CURRENT_SOURCE_DIR}  ${TRUST_ROOT}/include/backward  ${TRUST_ROOT}/src/Kernel/Framework  ${TRUST_ROOT}/src/Kernel/Geometrie  ${TRUST_ROOT}/src/Kernel/Math  ${TRUST_ROOT}/src/Kernel/Utilitaires )
add_library(obj_zvf OBJECT  ${srcs} )
set(listlibs ${listlibs} zvf PARENT_SCOPE    )
add_custom_target(check_sources_zvf   COMMAND check_sources.sh ${CMAKE_CURRENT_SOURCE_DIR} COMMENT  "checking code validity zvf" )
add_DEPENDENCIES(obj_zvf check_sources_zvf)
if(COMPIL_DYN)
  add_library(zvf SHARED   $<TARGET_OBJECTS:obj_zvf> )
  install(TARGETS zvf  DESTINATION lib)
endif(COMPIL_DYN)
