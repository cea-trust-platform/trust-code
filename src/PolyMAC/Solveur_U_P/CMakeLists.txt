file(GLOB srcs [A-Z|a-z]*.cpp [A-Z|a-z]*.c [A-Z|a-z]*.f)
include_directories(${CMAKE_CURRENT_SOURCE_DIR}  ${TRUST_ROOT}/include/backward  ${TRUST_ROOT}/src/Kernel/Cond_Lim  ${TRUST_ROOT}/src/Kernel/Framework  ${TRUST_ROOT}/src/Kernel/Geometrie  ${TRUST_ROOT}/src/Kernel/Math  ${TRUST_ROOT}/src/Kernel/Math/Matrices  ${TRUST_ROOT}/src/Kernel/Math/SolvSys  ${TRUST_ROOT}/src/Kernel/Operateurs  ${TRUST_ROOT}/src/Kernel/Schemas_Temps  ${TRUST_ROOT}/src/Kernel/Solveurs  ${TRUST_ROOT}/src/Kernel/Utilitaires  ${TRUST_ROOT}/src/Kernel/VF/Zones  ${TRUST_ROOT}/src/PolyMAC/Zones  ${TRUST_ROOT}/src/ThHyd/Schemas_Temps  ${TRUST_ROOT}/src/ThHyd)
add_library(obj_polymac_solveur_u_p OBJECT  ${srcs} )
set(listlibs ${listlibs} polymac_solveur_u_p PARENT_SCOPE    )
add_custom_target(check_sources_polymac_solveur_u_p   COMMAND check_sources.sh ${CMAKE_CURRENT_SOURCE_DIR} ) #COMMENT  "checking code validity polymac_solveur_u_p" )
add_DEPENDENCIES(obj_polymac_solveur_u_p check_sources_polymac_solveur_u_p)
if(COMPIL_DYN)
  add_library(polymac_solveur_u_p SHARED   $<TARGET_OBJECTS:obj_polymac_solveur_u_p> )
  install(TARGETS polymac_solveur_u_p  DESTINATION lib)
endif(COMPIL_DYN)
