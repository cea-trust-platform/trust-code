file(GLOB srcs [A-Z|a-z]*.cpp [A-Z|a-z]*.c [A-Z|a-z]*.f)
include_directories(${CMAKE_CURRENT_SOURCE_DIR}  ${TRUST_ROOT}/include/backward  ${TRUST_ROOT}/src/Kernel/Champs  ${TRUST_ROOT}/src/Kernel/Champs/Champs_Don  ${TRUST_ROOT}/src/Kernel/Cond_Lim  ${TRUST_ROOT}/src/Kernel/Framework  ${TRUST_ROOT}/src/Kernel/Geometrie  ${TRUST_ROOT}/src/Kernel/Math/Matrices  ${TRUST_ROOT}/src/Kernel/Math  ${TRUST_ROOT}/src/Kernel/Utilitaires  ${TRUST_ROOT}/src/Kernel/VF/Zones  ${TRUST_ROOT}/src/VEF/Zones )
add_library(obj_itsovef OBJECT  ${srcs} )
set(listlibs ${listlibs} itsovef PARENT_SCOPE    )
add_custom_target(check_sources_itsovef   COMMAND check_sources.sh ${CMAKE_CURRENT_SOURCE_DIR} ) #COMMENT  "checking code validity itsovef" )
add_DEPENDENCIES(obj_itsovef check_sources_itsovef)
if(COMPIL_DYN)
  add_library(itsovef SHARED   $<TARGET_OBJECTS:obj_itsovef> )
  install(TARGETS itsovef  DESTINATION lib)
endif(COMPIL_DYN)
