file(GLOB srcs [A-Z|a-z]*.cpp [A-Z|a-z]*.c [A-Z|a-z]*.f)
include_directories(${CMAKE_CURRENT_SOURCE_DIR}  ${TRUST_ROOT}/include/backward  ${TRUST_ROOT}/src/Kernel/Framework  ${TRUST_ROOT}/src/Kernel/Geometrie  ${TRUST_ROOT}/src/Kernel/Math  ${TRUST_ROOT}/src/Kernel/Utilitaires  ${TRUST_ROOT}/src/Kernel/VF/Zones  ${TRUST_ROOT}/src/VDF/Zones )
add_library(obj_elmtvdf OBJECT  ${srcs} )
set(listlibs ${listlibs} elmtvdf PARENT_SCOPE    )
add_custom_target(check_sources_elmtvdf   COMMAND check_sources.sh ${CMAKE_CURRENT_SOURCE_DIR} ) #COMMENT  "checking code validity elmtvdf" )
add_DEPENDENCIES(obj_elmtvdf check_sources_elmtvdf)
if(COMPIL_DYN)
  add_library(elmtvdf SHARED   $<TARGET_OBJECTS:obj_elmtvdf> )
  install(TARGETS elmtvdf  DESTINATION lib)
endif(COMPIL_DYN)
