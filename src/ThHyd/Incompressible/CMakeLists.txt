file(GLOB srcs [A-Z|a-z]*.cpp [A-Z|a-z]*.c [A-Z|a-z]*.f)
include_directories(${CMAKE_CURRENT_SOURCE_DIR}  ${TRUST_ROOT}/include/backward  ${TRUST_ROOT}/src/Kernel/Champs/Champs_Don  ${TRUST_ROOT}/src/Kernel/Framework  ${TRUST_ROOT}/src/Kernel/Geometrie  ${TRUST_ROOT}/src/Kernel/Math/Matrices  ${TRUST_ROOT}/src/Kernel/Math/SolvSys  ${TRUST_ROOT}/src/Kernel/Math  ${TRUST_ROOT}/src/Kernel/Operateurs  ${TRUST_ROOT}/src/Kernel/Schemas_Temps  ${TRUST_ROOT}/src/Kernel/Utilitaires  ${TRUST_ROOT}/src/Kernel/VF/Zones  ${TRUST_ROOT}/src/ThSol )
add_library(obj_thhydinc OBJECT  ${srcs} )
set(listlibs ${listlibs} thhydinc PARENT_SCOPE    )
add_custom_target(check_sources_thhydinc   COMMAND check_sources.sh ${CMAKE_CURRENT_SOURCE_DIR} ) #COMMENT  "checking code validity thhydinc" )
add_DEPENDENCIES(obj_thhydinc check_sources_thhydinc)
if(COMPIL_DYN)
  add_library(thhydinc SHARED   $<TARGET_OBJECTS:obj_thhydinc> )
  install(TARGETS thhydinc  DESTINATION lib)
endif(COMPIL_DYN)
