file(GLOB srcs [A-Z|a-z]*.cpp [A-Z|a-z]*.c [A-Z|a-z]*.f)
include_directories(${CMAKE_CURRENT_SOURCE_DIR}  ${TRUST_ROOT}/include/backward  ${TRUST_ROOT}/src/Kernel/Framework  ${TRUST_ROOT}/src/Kernel/Geometrie  ${TRUST_ROOT}/src/Kernel/Math/Matrices  ${TRUST_ROOT}/src/Kernel/Math/SolvSys  ${TRUST_ROOT}/src/Kernel/Math  ${TRUST_ROOT}/src/Kernel/Operateurs  ${TRUST_ROOT}/src/Kernel/Utilitaires  ${TRUST_ROOT}/src/ThHyd/Modele_turbulence_base  ${TRUST_ROOT}/src/ThHyd/Quasi_Compressible  ${TRUST_ROOT}/src/ThHyd/Turbulence  ${TRUST_ROOT}/src/ThHyd  ${TRUST_ROOT}/src/ThSol )
add_library(obj_thhydturbqc OBJECT  ${srcs} )
set(listlibs ${listlibs} thhydturbqc PARENT_SCOPE    )
add_custom_target(check_sources_thhydturbqc   COMMAND check_sources.sh ${CMAKE_CURRENT_SOURCE_DIR} ) #COMMENT  "checking code validity thhydturbqc" )
add_DEPENDENCIES(obj_thhydturbqc check_sources_thhydturbqc)
if(COMPIL_DYN)
  add_library(thhydturbqc SHARED   $<TARGET_OBJECTS:obj_thhydturbqc> )
  install(TARGETS thhydturbqc  DESTINATION lib)
endif(COMPIL_DYN)
