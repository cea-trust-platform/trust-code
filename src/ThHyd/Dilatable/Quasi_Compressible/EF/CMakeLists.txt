file(GLOB srcs [A-Z|a-z]*.cpp [A-Z|a-z]*.c [A-Z|a-z]*.f)
include_directories(${CMAKE_CURRENT_SOURCE_DIR}  ${TRUST_ROOT}/include/backward  ${TRUST_ROOT}/src/EF/Sources  ${TRUST_ROOT}/src/EF/Zones  ${TRUST_ROOT}/src/Kernel/Champs  ${TRUST_ROOT}/src/Kernel/Framework  ${TRUST_ROOT}/src/Kernel/Geometrie  ${TRUST_ROOT}/src/Kernel/Math/Matrices  ${TRUST_ROOT}/src/Kernel/Math  ${TRUST_ROOT}/src/Kernel/Utilitaires  ${TRUST_ROOT}/src/Kernel/VF/Zones  ${TRUST_ROOT}/src/ThSol )
add_library(obj_thhydqcEF OBJECT  ${srcs} )
set(listlibs ${listlibs} thhydqcEF PARENT_SCOPE    )
add_custom_target(check_sources_thhydqcEF   COMMAND check_sources.sh ${CMAKE_CURRENT_SOURCE_DIR} ) #COMMENT  "checking code validity thhydqcEF" )
add_DEPENDENCIES(obj_thhydqcEF check_sources_thhydqcEF)
if(COMPIL_DYN)
  add_library(thhydqcEF SHARED   $<TARGET_OBJECTS:obj_thhydqcEF> )
  install(TARGETS thhydqcEF  DESTINATION lib)
endif(COMPIL_DYN)
