file(GLOB srcs [A-Z|a-z]*.cpp [A-Z|a-z]*.c [A-Z|a-z]*.f)
include_directories(${CMAKE_CURRENT_SOURCE_DIR}  ${TRUST_ROOT}/include/backward  ${TRUST_ROOT}/src/Kernel/Champs_dis  ${TRUST_ROOT}/src/Kernel/Cond_Lim  ${TRUST_ROOT}/src/Kernel/Framework  ${TRUST_ROOT}/src/Kernel/Geometrie  ${TRUST_ROOT}/src/Kernel/Math/Matrices  ${TRUST_ROOT}/src/Kernel/Math/SolvSys  ${TRUST_ROOT}/src/Kernel/Math  ${TRUST_ROOT}/src/Kernel/Utilitaires  ${TRUST_ROOT}/src/Kernel/VF/Zones  ${TRUST_ROOT}/src/P1NCP1B/Zones  ${TRUST_ROOT}/src/ThHyd/Incompressible/Champs  ${TRUST_ROOT}/src/ThHyd/Incompressible/Cond_Lim  ${TRUST_ROOT}/src/ThHyd/Incompressible/Equations  ${TRUST_ROOT}/src/ThHyd/Incompressible/Milieu  ${TRUST_ROOT}/src/ThHyd/Incompressible/Problems  ${TRUST_ROOT}/src/ThHyd/Incompressible/Sources  ${TRUST_ROOT}/src/ThHyd/Incompressible/Traitement_particulier  ${TRUST_ROOT}/src/ThHyd/Incompressible  ${TRUST_ROOT}/src/ThHyd  ${TRUST_ROOT}/src/VEF/Champs  ${TRUST_ROOT}/src/VEF/Zones )
add_library(obj_chvefp1b OBJECT  ${srcs} )
set(listlibs ${listlibs} chvefp1b PARENT_SCOPE    )
add_custom_target(check_sources_chvefp1b   COMMAND check_sources.sh ${CMAKE_CURRENT_SOURCE_DIR} ) #COMMENT  "checking code validity chvefp1b" )
add_DEPENDENCIES(obj_chvefp1b check_sources_chvefp1b)
if(COMPIL_DYN)
  add_library(chvefp1b SHARED   $<TARGET_OBJECTS:obj_chvefp1b> )
  install(TARGETS chvefp1b  DESTINATION lib)
endif(COMPIL_DYN)
