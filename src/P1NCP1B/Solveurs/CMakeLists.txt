file(GLOB srcs [A-Z|a-z]*.cpp [A-Z|a-z]*.c [A-Z|a-z]*.f)
include_directories(${CMAKE_CURRENT_SOURCE_DIR}  ${TRUST_ROOT}/include/backward  ${TRUST_ROOT}/lib/src/LIBAMGX/AmgXWrapper/include  ${TRUST_ROOT}/lib/src/LIBAMGX/AmgX/include  ${CUDA_INC_PATH}  ${TRUST_ROOT}/src/Kernel/Champs_dis  ${TRUST_ROOT}/src/Kernel/Cond_Lim  ${TRUST_ROOT}/src/Kernel/Framework  ${TRUST_ROOT}/src/Kernel/Geometrie  ${TRUST_ROOT}/src/Kernel/Math/Matrices  ${TRUST_ROOT}/src/Kernel/Math/SolvSys  ${TRUST_ROOT}/src/Kernel/Math  ${TRUST_ROOT}/src/Kernel/Operateurs  ${TRUST_ROOT}/src/Kernel/Utilitaires  ${TRUST_ROOT}/src/Kernel/VF/Zones  ${TRUST_ROOT}/src/P1NCP1B/Champs  ${TRUST_ROOT}/src/P1NCP1B/Operateurs  ${TRUST_ROOT}/src/P1NCP1B/Zones  ${TRUST_ROOT}/src/ThHyd  ${TRUST_ROOT}/src/ThSol  ${TRUST_ROOT}/src/VEF/Champs  ${TRUST_ROOT}/src/VEF/Operateurs  ${TRUST_ROOT}/src/VEF/Solveurs  ${TRUST_ROOT}/src/VEF/Zones )
add_library(obj_solvefp1b OBJECT  ${srcs} )
set(listlibs ${listlibs} solvefp1b PARENT_SCOPE    )
add_custom_target(check_sources_solvefp1b   COMMAND check_sources.sh ${CMAKE_CURRENT_SOURCE_DIR} ) #COMMENT  "checking code validity solvefp1b" )
add_DEPENDENCIES(obj_solvefp1b check_sources_solvefp1b)
if(COMPIL_DYN)
  add_library(solvefp1b SHARED   $<TARGET_OBJECTS:obj_solvefp1b> )
  install(TARGETS solvefp1b  DESTINATION lib)
endif(COMPIL_DYN)
